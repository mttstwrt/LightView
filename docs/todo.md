1. Add RGBA option for thumbnails, set it as the default.

2. Add thumbnail dimension imput to thumbnail section in settings.

2. Add thumbnail details to the info panel on an image, include dimensions and which algorithm was used to generate it

2. Add adjustable blur effect when scrolling

2. Multi selection with the control key, tag many images at once

3. Currently it doesn't seem to 'remember' tags in the database, after restarting the app typing in a tag does not give suggestions or autocomplete

4. Add sort by rating and filter by rating

5. Add blur effect when scrolling, might help with the smooth feeling UI. 

6. Add fps counter and other live performance metrics into debug menu. Goal is to be *smooth* and no visual issues

7. Certain images continue to not generate thumbnails, it is always the same ones.

8. Replace ambiguous 'thumbnail quality' slider with direct dimension input and algorithm choice
